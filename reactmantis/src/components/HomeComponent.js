import React from 'react';
import { Card, CardText } from 'reactstrap';

function Home(props) {
    return (
        <Card>
            <CardText>This is a Home, stand-in page.</CardText>
        </Card>
    );
}

export default Home;